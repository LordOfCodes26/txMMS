<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialogRadioButtonHolder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.goodwy.commons.views.MyCompatRadioButton
        android:id="@+id/dialogRadioButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:buttonTint="@color/tx_dialog_confirm_text_color"
        android:theme="@style/Theme.Common"
        android:layout_toStartOf="@+id/dialogRadioButtonIcon"
        android:textDirection="locale"
        android:textAlignment="viewStart" />

    <ImageView
        android:id="@+id/dialogRadioButtonIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd='true'
        android:layout_centerVertical='true'
        android:layout_marginEnd="@dimen/small_margin"/>
</RelativeLayout>
