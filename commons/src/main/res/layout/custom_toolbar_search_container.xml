<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/searchContainer"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical|end"
    android:visibility="gone">

    <ImageButton
        android:id="@+id/searchBackButton"
        android:layout_width="@dimen/medium_icon_size"
        android:layout_height="@dimen/medium_icon_size"
        android:layout_marginEnd="@dimen/smaller_margin"
        android:scaleType="centerInside"
        android:padding="@dimen/icon_padding"
        android:src="@drawable/ic_cmn_arrow_left"
        android:contentDescription="@string/back"
        android:background="@null"
        android:visibility="gone"
        tools:visibility="visible" />

    <com.goodwy.commons.views.MyEditText
        android:id="@+id/searchEditText"
        android:layout_width="0dp"
        android:layout_height="33dp"
        android:layout_weight="1"
        android:layout_marginEnd="@dimen/smaller_margin"
        android:hint="@string/search"
        android:maxLines="1"
        android:gravity="start|center_vertical"
        android:background="@drawable/search_bg"
        android:paddingStart="@dimen/normal_margin"
        android:paddingEnd="@dimen/normal_margin"
        android:textSize="@dimen/normal_text_size"
        android:drawableStart="@drawable/ic_cmn_search"
        android:drawableEnd="@drawable/ic_cmn_search_close"
        android:drawablePadding="@dimen/smaller_margin"
        tools:text="Search text" />

</LinearLayout>
