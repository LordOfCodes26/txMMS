<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/vcard_strip_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="@dimen/small_margin"
    android:paddingTop="@dimen/tiny_margin"
    android:paddingEnd="@dimen/small_margin"
    android:paddingBottom="@dimen/tiny_margin"
    android:visibility="gone"
    tools:visibility="visible">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/vcard_strip_photo"
        android:layout_width="@dimen/medium_icon_size"
        android:layout_height="@dimen/medium_icon_size"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/small_margin"
        android:layout_weight="1"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/vcard_strip_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textSize="@dimen/normal_text_size"
            android:textStyle="bold"
            tools:text="vCard (John Doe)" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/vcard_strip_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textSize="@dimen/smaller_text_size"
            android:visibility="gone"
            tools:text="and 2 others"
            tools:visibility="visible" />

    </LinearLayout>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/vcard_strip_remove"
        android:layout_width="@dimen/remove_attachment_size"
        android:layout_height="@dimen/remove_attachment_size"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="@dimen/tiny_margin"
        android:scaleType="centerInside"
        android:src="@drawable/ic_delete_restore"
        tools:ignore="ContentDescription" />

</LinearLayout>
